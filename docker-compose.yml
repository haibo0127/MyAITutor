version: '3.8'

services:
  ai-tutor:
    build: .
    restart: always
    ports:
      - "8000:8000"
      - "80:80"
    volumes:
      - ./data:/app/data
      - ./nginx.conf:/etc/nginx/nginx.conf
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=1